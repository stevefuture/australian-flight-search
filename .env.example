# Flight API credentials (optional - app works with mock data by default)
# SERPAPI_KEY=your_serpapi_key_here

# Server configuration
PORT=3000

# Cache configuration (reduces API costs by caching search results)
CACHE_TTL_MINUTES=10
