services:
  - type: web
    name: au-flight-search
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: SERPAPI_KEY
        sync: false
      - key: CACHE_TTL_MINUTES
        value: 10
